<h2>Timesheet</h2>

<p>
<% form_for :date, :url =>{:action => 'index', :id => @project} do |f|  %>

<p><label for="date_from">From:</label><br />
<%= f.text_field "from", :size => 10, :value => @from %><%= calendar_for('date_from') %></p>

<p><label for="date_to">To:</label><br />
<%= f.text_field "to", :size => 10, :value => @to %><%= calendar_for('date_to') %></p>

<%= select("project","id", Project.find(:all).collect {|p| [ p.name, p.id ] }, { :include_blank => true }) %>


<%= submit_tag "Get results" -%>
<% end %>


</p>

<% if @entries.length > 0 %>
<h2><%= l(:label_spent_time) %>(<%= @total -%>hours)</h2>

<% @entries.each do |entryname,entry| -%>
<%= render :partial => 'project_timesheet', :locals => {:entry => entry, :name => entryname} %>
<% end -%>
<% end -%>


<% content_for :header_tags do %>
    <%= javascript_include_tag 'calendar/calendar' %>
    <%= javascript_include_tag "calendar/lang/calendar-#{current_language}.js" %>
    <%= javascript_include_tag 'calendar/calendar-setup' %>
    <%= stylesheet_link_tag 'calendar' %>
<% end %>

